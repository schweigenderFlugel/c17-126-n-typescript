import { DashboardNavbar } from "./DashboardNavbar"

export const TransfersList = () => {
  return (
    <main className="w-full h-full grid grid-cols-12 grid-rows-12 gap-y-10 bg-transparent">
      <DashboardNavbar>Transferencias</DashboardNavbar>
    </main>
  )
}